# ========================================
# Required Secrets (Set in Codespace Secrets)
# ========================================

# OpenAI API Key (Required)
OPENAI_API_KEY=sk-proj-...

# Google OAuth Credentials (Required for Google Calendar features)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Session Encryption Key (Optional, auto-generated if not set)
# SESSION_SECRET=your_random_secret_here

# Google Webhook Token (Optional, for webhooks)
# GOOGLE_WEBHOOK_TOKEN=your_webhook_token_here

# Notion API Key (Optional, for Notion integration)
# NOTION_API_KEY=your_notion_api_key_here
# NOTION_TOKEN=your_notion_token_here

# Context7 API Key (Optional, for Context7 integration)
# CONTEXT7_API_KEY=your_context7_api_key_here


# ========================================
# Auto-Configured Environment Variables (No Setup Required)
# ========================================
# The following variables are automatically set by the dev-run.sh script.
# In Codespaces, it auto-detects the current Codespace URL.
# In local environment, it uses localhost.

# BACKEND_PUBLIC_BASE (auto-configured)
# FRONTEND_BASE_URL (auto-configured)
# GOOGLE_REDIRECT_URI (auto-configured: ${FRONTEND_BASE_URL}/auth/google/callback)
# GOOGLE_WEBHOOK_URL (auto-configured: ${BACKEND_PUBLIC_BASE}/auth/google/webhook)
# CORS_ALLOW_ORIGINS (auto-configured)
# COOKIE_SECURE (auto-configured: Codespaces=1, local=0)
# NEXT_PUBLIC_API_BASE (auto-configured: /api)
# NEXT_PUBLIC_BACKEND_DIRECT (auto-configured: ${BACKEND_PUBLIC_BASE})


# ========================================
# Optional Settings (Default values recommended)
# ========================================

# Enable Google Calendar features (default: 1)
# ENABLE_GCAL=1

# LLM debug mode (default: 0)
# LLM_DEBUG=0

# Google token file path (default: backend/.gcal_tokens)
# GOOGLE_TOKEN_FILE=backend/.gcal_tokens

# Google Calendar ID (default: primary)
# GOOGLE_CALENDAR_ID=primary
